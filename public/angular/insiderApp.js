/*angular.module('insiderApp', []);

app.controller('ArticlesController', function($scope) {
    var vm = this;
    vm.items = articles;
    vm.limit = 5;

    $scope.loadMore = function() {
      var increamented = vm.limit + 3;
      vm.limit = incremented > vm.items.length ? vm.items.length : increamented;
    };
});*/